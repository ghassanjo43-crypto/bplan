fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.10.0
python-dateutil>=2.9.0
python-multipart>=0.0.9
# Authentication
PyJWT>=2.8.0
bcrypt>=4.1.0
# Word reports
python-docx>=1.1.0
# Renders the Financial Analysis charts to PNG images for the Word/PDF reports.
matplotlib>=3.7.0
# Topic image handling (dimensions, validation)
Pillow>=10.0.0
# PDF rendering (needs native libs: Pango/Cairo — installed in the Dockerfile).
weasyprint>=60.0
openpyxl>=3.1.5,<4